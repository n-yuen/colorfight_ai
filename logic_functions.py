def cell_danger(cell):
    x, y = cell.position.x, cell.position.y
    for pos_x in range(x - 5, x + 5):
        for pos_y in range(y - 5, y + 5):
            try:
                if 

    pass


def
